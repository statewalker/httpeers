/**
 * The one generated snippet biscuit-wasm's binary imports, typed for the
 * alias. Vite resolves `#biscuit-snippet` to the real file; TypeScript needs
 * to be told what shape comes back, and the module is two lines.
 */
declare module "#biscuit-snippet" {
  export function performance_now(): number;
}
