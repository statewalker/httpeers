# GENERATED by bin/prepare.sh -- do not edit by hand.

services:
  llamacpp-small-a:
    image: ghcr.io/ggml-org/llama.cpp:server-musa
    restart: unless-stopped
    networks:
      - appliance
    volumes:
      - ${APPLIANCE_MODELS_DIR:-./models}/small-a/a.gguf:/models/small-a/a.gguf:ro
    devices:
      - "/dev/mtgpu:/dev/mtgpu"
      - "/dev/dri:/dev/dri"
    command:
      - --model
      - /models/small-a/a.gguf
      - --alias
      - small-a
      - --host
      - 0.0.0.0
      - --port
      - "8080"
      - --ctx-size
      - "4096"
      - --n-gpu-layers
      - "999"
      - --verbosity
      - "4"
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:8080/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 60
      start_period: 300s

  llamacpp-small-b:
    image: ghcr.io/ggml-org/llama.cpp:server-musa
    restart: unless-stopped
    networks:
      - appliance
    volumes:
      - ${APPLIANCE_MODELS_DIR:-./models}/small-b/b.gguf:/models/small-b/b.gguf:ro
    devices:
      - "/dev/mtgpu:/dev/mtgpu"
      - "/dev/dri:/dev/dri"
    command:
      - --model
      - /models/small-b/b.gguf
      - --alias
      - small-b
      - --host
      - 0.0.0.0
      - --port
      - "8080"
      - --ctx-size
      - "8192"
      - --n-gpu-layers
      - "999"
      - --verbosity
      - "4"
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:8080/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 60
      start_period: 300s
